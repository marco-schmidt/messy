apply plugin: 'java-library'

description 'library with data classes for various electronic message formats'

dependencies {
  implementation libs.slf4j.api
  implementation libs.logback.classic
  testImplementation libs.junit
}
